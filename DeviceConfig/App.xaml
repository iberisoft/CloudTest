<Application x:Class="DeviceConfig.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:DeviceConfig"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ObjectDataProvider x:Key="Settings" ObjectInstance="{x:Static local:Settings.Default}" />
        <local:NotNullToBoolConverter x:Key="NotNullToBoolConverter" />
        <Style x:Key="WindowStyle" TargetType="Window">
            <Setter Property="FontSize" Value="15" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="3" />
            <Setter Property="Padding" Value="5" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="3" />
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="Margin" Value="3" />
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Margin" Value="3" />
        </Style>
        <Style TargetType="ListBox">
            <Setter Property="Margin" Value="3" />
        </Style>
        <Style TargetType="GroupBox">
            <Setter Property="Margin" Value="3" />
        </Style>
    </Application.Resources>
</Application>
